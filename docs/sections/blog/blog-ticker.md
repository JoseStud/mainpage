# Blog Ticker

Purpose: Holds the short marquee line that sets the tone between the archive intro and the recent entries list.

Important anchors/classes: `.marquee-box`, `marquee`, `aria-label="Blog ticker"`.

When to edit: Change this file when the ticker copy or marquee behavior changes. Keep the marquee wrapper intact unless the page is deliberately dropping that effect.
