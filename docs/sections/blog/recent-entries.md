# Recent Entries

Purpose: Renders the latest five entry cards with titles, dates, states, and one-line summaries.

Important anchors/classes: `.entry-list`, `.entry-item`, `.entry-top`, `.entry-title`, `.entry-state`, `.entry-summary`, `href="#reading-desk"`.

When to edit: Use this file for adding, removing, or reordering the visible recent entries. Preserve the link target and entry class names so styling and in-page navigation stay stable.
