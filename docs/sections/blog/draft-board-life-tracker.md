# Draft Board + Life Tracker

Purpose: Keeps the two-column `portal-split` chunk together for draft ideas and the current reading/habits desk notes.

Important anchors/classes: `.portal-split`, `#draft-board`, `#life-tracker`, `.note-list`, `.subsection-block`, `.subsection-list`.

When to edit: Update this file for either side of the split when the pair should continue to share the same layout wrapper. Keep both section IDs because they are stable anchors referenced elsewhere.
