# Reading Desk Intro

Purpose: Opens the blog archive with the `/blog.log` heading, intro copy, post count, and archive map.

Important anchors/classes: `#reading-desk`, `.box`, `.box-heading`, `.box-content`, `.subsection-block`, `.subsection-list`.

When to edit: Update this file for the intro paragraph, total entry count, or year breakdown. Keep `id="reading-desk"` because the recent entry links point here.
