# Background-only sunrise stage

This section is reused by the sunrise test suite, with page runtime toggles deciding which background layers are active.

- Starts in dark mode every load.
- Autoplays the sunrise transition shortly after paint.
- Lets each generated page add one more background layer than the previous one.
