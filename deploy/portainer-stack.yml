services:
  mainpage:
    image: ghcr.io/josestud/mainpage:939b757b7ace99729578314d1bf1d9a6864f0e51
    container_name: mainpage
    restart: unless-stopped
    pull_policy: always
    environment:
      - MULTI_SCROBBLER_BASE_URL=http://multi-scrobbler:9078
    ports:
      - "8000:80"
    depends_on:
      - multi-scrobbler

  multi-scrobbler:
    image: ghcr.io/josestud/mainpage:939b757b7ace99729578314d1bf1d9a6864f0e51
    container_name: multi-scrobbler
    restart: unless-stopped
    ports:
      - "9078:9078"
    volumes:
      - multi_scrobbler_config:/config

volumes:
  multi_scrobbler_config:
